const assert = require("chai").assert
const Meal = require("../lib/js/meal")
const pry = require('pryjs')

describe('Food#filterAll', function() {
  this.timeout(100000)
  it('can filter an array of foods based on a partial input', function(done) {
    
    
    assert.
    done()
  })
})